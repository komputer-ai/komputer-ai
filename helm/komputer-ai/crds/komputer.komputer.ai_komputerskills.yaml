---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: komputerskills.komputer.komputer.ai
spec:
  group: komputer.komputer.ai
  names:
    kind: KomputerSkill
    listKind: KomputerSkillList
    plural: komputerskills
    singular: komputerskill
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.description
      name: Description
      type: string
    - jsonPath: .status.attachedAgents
      name: Agents
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          KomputerSkill is a reusable skill that can be attached to agents.
          Skills are written to the agent's filesystem as Claude SDK skill files.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KomputerSkillSpec defines the desired state of KomputerSkill.
            properties:
              content:
                description: Content is the skill's markdown body (instructions/prompt).
                type: string
              description:
                description: Description is a short description of when to use this
                  skill.
                type: string
            required:
            - content
            - description
            type: object
          status:
            description: KomputerSkillStatus defines the observed state of KomputerSkill.
            properties:
              agentNames:
                description: AgentNames is the list of agent names that reference
                  this skill.
                items:
                  type: string
                type: array
              attachedAgents:
                description: AttachedAgents is the number of agents that reference
                  this skill.
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
