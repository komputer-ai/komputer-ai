{{- if .Values.agent.serviceAccount.create }}
{{- $saName := include "komputer.agent.serviceAccountName" . }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.agent.serviceAccount.clusterWide }}
kind: ClusterRole
{{- else }}
kind: Role
{{- end }}
metadata:
  name: {{ $saName }}
  {{- if not .Values.agent.serviceAccount.clusterWide }}
  namespace: {{ .Release.Namespace }}
  {{- end }}
  labels:
    {{- include "komputer.labels" . | nindent 4 }}
    app.kubernetes.io/component: agent
rules:
  {{- toYaml .Values.agent.serviceAccount.rules | nindent 2 }}
---
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.agent.serviceAccount.clusterWide }}
kind: ClusterRoleBinding
{{- else }}
kind: RoleBinding
{{- end }}
metadata:
  name: {{ $saName }}
  {{- if not .Values.agent.serviceAccount.clusterWide }}
  namespace: {{ .Release.Namespace }}
  {{- end }}
  labels:
    {{- include "komputer.labels" . | nindent 4 }}
    app.kubernetes.io/component: agent
roleRef:
  apiGroup: rbac.authorization.k8s.io
  {{- if .Values.agent.serviceAccount.clusterWide }}
  kind: ClusterRole
  {{- else }}
  kind: Role
  {{- end }}
  name: {{ $saName }}
subjects:
- kind: ServiceAccount
  name: {{ $saName }}
  namespace: {{ .Release.Namespace }}
{{- end }}
